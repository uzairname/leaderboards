export enum events {
  MatchFinished = 'match finished', // New match is added to the database, and players' ratings were updated
  RankingUpdated = 'ranking updated', // Ranking was renamed or modified
  GuildRankingUpdated = 'guild ranking updated', // Guild
}
