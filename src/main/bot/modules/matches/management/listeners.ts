import { AppEvents } from '../../../../app/AppEvents'

export default function (events: AppEvents) {
  // events.MatchCreatedOrUpdated.on(async (app, match) => {
  //   await events.RankingLeaderboardUpdated.emit(await match.ranking())
  // })
}
